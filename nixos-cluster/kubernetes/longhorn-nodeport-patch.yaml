apiVersion: v1
kind: Service
metadata:
  name: longhorn-frontend
  namespace: longhorn-system
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30880
  selector:
    app: longhorn-ui
